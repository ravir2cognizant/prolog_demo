---
id: TC-BFF-003
ed-ref: ED-001
rc-ref: RC-001
type: contract
priority: P2
automated: yes
---

# TC-BFF-003 — Pact Contract for NavItem Response Shape

## Scenario
Consumer-driven contract test verifying the BFF fulfils the FE's NavItem expectations.

## Preconditions
- Pact consumer contract generated by FE tests

## Steps
1. Run Pact provider verification against BFF
2. Verify BFF publishes correct `items[*].id` (string), `label` (string), `route` (string), `level` (integer 0|1), `alertState` (boolean), `enabled` (boolean)
3. Assert all Pact interactions pass

## Expected Result
- All Pact interactions verified
- No shape mismatches

## Test Data
- Pact contract from FE consumer tests (generated during T-011)
